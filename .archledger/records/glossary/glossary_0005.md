---
id: glossary_0005
type: glossary_term
title: "Dialect"
status: accepted
section: glossary
order: 50
term: "Dialect"
definition: "A source format abstraction that defines how to render markup elements (headings, tables, bullets, strong text). archledger provides MarkdownDialect and AsciiDocDialect."
---

A source format abstraction that defines how to render markup elements (headings, tables, bullets, strong text). archledger provides `MarkdownDialect` and `AsciiDocDialect`. All section rendering uses dialects to produce format-correct output.
